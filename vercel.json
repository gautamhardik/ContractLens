{
  "version": 2,
  "framework": "vite",
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "env": {
    "VITE_API_BASE": {
      "description": "URL of the deployed Railway backend (e.g. https://contractlens-backend.up.railway.app)",
      "required": true
    }
  }
}
